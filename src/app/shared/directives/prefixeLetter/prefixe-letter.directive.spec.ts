import { PrefixeLetterDirective } from './prefixe-letter.directive';

describe('PrefixeLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new PrefixeLetterDirective();
    expect(directive).toBeTruthy();
  });
});
